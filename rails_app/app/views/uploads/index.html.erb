<%= render 'shared/nav' %>
<div class="hero bg-base-200">
  <div class="flex-col gap-x-5 gap-y-10 p-10" id="all-uploads-results">
    <%= render 'uploads/uploads_results', uploads: @uploads %>
  </div>
</div>