<%= render 'shared/nav' %>
<div class="hero bg-base-200">
  <div class="flex-col gap-x-5 gap-y-10 p-10">
    <%= render "uploads/status_badge", upload: @upload %>
    <%= render partial: 'details_card', locals: { upload: @upload } %>
  </div>
</div>
