service: receivers
frameworkVersion: '3'
useDotenv: true

provider:
  name: aws
  stage: ${opt:stage, 'test'}
  region: ${env:AWS_REGION}
  profile: ${env:AWS_PROFILE}
  runtime: python3.10
  timeout: 240
  architecture: arm64
  stackTags:
    product: receiver-functions
    customer-impact: true
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:ListBucket
            - s3:GetObject
            - s3:PutObject
          Resource:
            - arn:aws:s3:::${env:APP_NAME}-${opt:stage, 'test'}
            - arn:aws:s3:::${env:APP_NAME}-${opt:stage, 'test'}/*
            - arn:aws:s3:::${env:APP_NAME}-trigger
            - arn:aws:s3:::${env:APP_NAME}-trigger/*
        - Effect: Allow
          Action:
            - sqs:*
          Resource: "*"
  deploymentBucket:
    name: ${env:APP_NAME}-serverless-artifacts
  environment:
    APP_NAME: ${env:APP_NAME}
    STAGE: ${opt:stage, 'test'}

functions:
  receiver_start:
    name: ${env:APP_NAME}-receiver_start-${opt:stage, 'test'}
    image: ${env:ACCOUNT_ID}.dkr.ecr.us-west-2.amazonaws.com/${env:APP_NAME}-receiver_start:${opt:stage, 'test'}
    description: 'test rails receiver lambda function'
    environment:
      RECEIVER_NAME: receiver_start

  receiver_preprocess:
    name: ${env:APP_NAME}-receiver_preprocess-${opt:stage, 'test'}
    image: ${env:ACCOUNT_ID}.dkr.ecr.us-west-2.amazonaws.com/${env:APP_NAME}-receiver_preprocess:${opt:stage, 'test'}
    description: 'receiver preprocess lambda function'
    events:
      - sqs:
          arn: ${env:SQS_ARN_ROOT}${env:APP_NAME}-receiver_preprocess
          batchSize: 1
    environment:
      RECEIVER_NAME: receiver_preprocess


#   receiver_step_1:
#     image: ${env:ACCOUNT_ID}.dkr.ecr.us-west-2.amazonaws.com/${env:APP_NAME}-receiver_step_1:${opt:stage, 'development'}
#     description: ' receiver step 1 lambda function'
#     events:
#       - sqs:
#           arn: ${env:SQS_ARN_ROOT}${env:QUEUE_RECEIVER_STEP_1}
#           batchSize: 1
#     environment:
#       STAGE: ${opt:stage, 'dev'}
#       KRIXIK_URL: ${env:KRIXIK_URL}
#       KRIXIK_KEY: ${env:KRIXIK_KEY}


#   receiver_end:
#     image: ${env:ACCOUNT_ID}.dkr.ecr.us-west-2.amazonaws.com/${env:APP_NAME}-receiver_end:${opt:stage, 'development'}
#     description: 'end receiver lambda function'
#     events:
#       - sqs:
#           arn: ${env:SQS_ARN_ROOT}${env:QUEUE_RECEIVER_END}
#           batchSize: 1
#     environment:
#       STAGE: ${opt:stage, 'dev'}
#       BUCKET_MAIN: ${env:BUCKET_MAIN}
#       BUCKET_TEST: ${env:BUCKET_TEST}
#       BUCKET_TRIGGER: ${env:BUCKET_TRIGGER}